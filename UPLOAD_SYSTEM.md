# 📤 نظام الرفع والمعالجة - Upload & Processing System

## ✅ تم إنشاء نظام رفع ومعالجة كامل!

### 🎯 المميزات:

#### 1. **رفع الملفات**
- ✅ دعم الفيديو: MP4, MOV, AVI, MKV
- ✅ دعم الصوت: MP3, WAV, AAC, M4A
- ✅ حجم أقصى: 500MB
- ✅ رفع حقيقي مع progress bar

#### 2. **المعالجة التلقائية**
عند رفع الملف، يتم تلقائياً:

1. **استخراج Metadata**
   - المدة الزمنية
   - الدقة (للفيديو)
   - معدل البت
   - الترميز

2. **توليد Thumbnail**
   - صورة مصغرة بدقة 1280x720
   - من 10% من بداية الفيديو
   - تلقائياً للفيديو والصوت

3. **استخراج الصوت** (للفيديو فقط)
   - تحويل لـ MP3
   - جودة 192kbps
   - ملف منفصل للتشغيل

4. **إنشاء HLS Streaming**
   - تقسيم لـ segments
   - playlist.m3u8
   - جاهز للبث المباشر
   - دقات متعددة (قريباً)

5. **تحديث قاعدة البيانات**
   - حفظ جميع الروابط
   - تحديث الحالة
   - جاهز للعرض

---

## 🔧 Backend APIs

### POST `/api/upload/video`
رفع ملف فيديو أو صوت

**Headers:**
```
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

**Body (FormData):**
```
file: File (required)
title: string (required)
description: string (optional)
performerId: string (required)
```

**Response:**
```json
{
  "message": "تم رفع الملف بنجاح وجاري المعالجة",
  "contentId": "uuid",
  "status": "processing"
}
```

### POST `/api/upload/check-status/:id`
التحقق من حالة المعالجة

**Response:**
```json
{
  "status": "processing|completed|failed",
  "progress": 75,
  "currentStep": "تم إنشاء HLS",
  "steps": [
    "تم استخراج المعلومات",
    "تم إنشاء الصورة المصغرة",
    "تم استخراج الصوت",
    "تم إنشاء HLS"
  ]
}
```

---

## 📁 هيكل الملفات

```
uploads/
├── original/              # الملفات الأصلية
│   └── {timestamp}-{random}.mp4
│
└── processed/            # الملفات المعالجة
    └── {contentId}/
        ├── thumbnail.jpg     # الصورة المصغرة
        ├── audio.mp3        # الصوت المستخرج
        └── hls/             # HLS streaming
            ├── playlist.m3u8
            ├── segment0.ts
            ├── segment1.ts
            └── ...
```

---

## 🎨 لوحة التحكم

### صفحة Upload المحسّنة:

1. **اختيار الملف**
   - Drag & Drop (قريباً)
   - معاينة الملف
   - عرض الحجم والنوع

2. **معلومات المحتوى**
   - العنوان (مطلوب)
   - الوصف (اختياري)
   - المؤدي (مطلوب)

3. **Progress Bar**
   - رفع الملف (0-100%)
   - حالة المعالجة
   - الخطوة الحالية

4. **رسائل النجاح/الفشل**
   - تأكيد الرفع
   - حالة المعالجة
   - أخطاء واضحة

---

## 🚀 كيفية الاستخدام

### 1. افتح لوحة التحكم:
```
http://localhost:5173
```

### 2. سجل دخول:
- البريد: `admin@zawamel.com`
- كلمة المرور: `admin123`

### 3. اذهب لصفحة "رفع محتوى"

### 4. ارفع ملف:
- اختر فيديو أو صوت
- أدخل العنوان
- اختر المؤدي
- اضغط "رفع الملف"

### 5. انتظر المعالجة:
- الرفع: 10-30 ثانية
- المعالجة: 1-5 دقائق (حسب الحجم)

### 6. جاهز!
- الملف معالج بالكامل
- جاهز للعرض في التطبيق
- جميع الصيغ متوفرة

---

## 📊 المتطلبات

### Backend:
- ✅ FFmpeg مثبت على السيرفر
- ✅ مساحة تخزين كافية
- ✅ Node.js مع NestJS

### Packages:
```bash
npm install @nestjs/platform-express fluent-ffmpeg
npm install --save-dev @types/multer @types/fluent-ffmpeg
```

---

## ⚠️ ملاحظات مهمة

1. **FFmpeg:**
   - يجب تثبيت FFmpeg على السيرفر
   - Windows: تحميل من ffmpeg.org
   - Linux: `sudo apt install ffmpeg`
   - Mac: `brew install ffmpeg`

2. **الأداء:**
   - المعالجة تتم في الخلفية
   - لا تؤثر على الرفع
   - يمكن رفع ملفات متعددة

3. **التخزين:**
   - الملفات الأصلية تبقى
   - الملفات المعالجة منفصلة
   - يمكن حذف الأصلية لاحقاً

4. **الأمان:**
   - فقط المسؤولين يمكنهم الرفع
   - فحص نوع الملف
   - حد أقصى للحجم

---

## 🎯 التطويرات المستقبلية

- [ ] دقات متعددة (360p, 480p, 720p, 1080p)
- [ ] Drag & Drop للرفع
- [ ] معاينة الفيديو قبل الرفع
- [ ] رفع متعدد (Batch Upload)
- [ ] استئناف الرفع عند الانقطاع
- [ ] ضغط تلقائي للملفات الكبيرة
- [ ] Watermark تلقائي
- [ ] Subtitles extraction

---

**الآن يمكنك رفع محتوى حقيقي واختباره على Android!** 🚀
