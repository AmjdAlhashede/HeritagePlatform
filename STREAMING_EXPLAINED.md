# شرح نظام البث والتحميل

## 🎯 الهدف
مثل YouTube بالضبط:
- المستخدم يشغل الفيديو → الدقة تتغير تلقائياً
- المستخدم يحمل الفيديو → يختار الدقة اللي يبيها

## 📦 ما يتم إنشاؤه عند الرفع

### 1. HLS للمشاهدة (Adaptive Streaming)
```
uploads/processed/{contentId}/hls/
├── playlist.m3u8          # الملف الرئيسي
├── 1080p.m3u8            # قائمة 1080p
├── 1080p_0.ts            # قطع الفيديو 1080p
├── 1080p_1.ts
├── 720p.m3u8             # قائمة 720p
├── 720p_0.ts             # قطع الفيديو 720p
├── 480p.m3u8             # قائمة 480p
└── 480p_0.ts             # قطع الفيديو 480p
```

**كيف يشتغل:**
- المشغل يفتح `playlist.m3u8`
- يشوف الدقات المتاحة (1080p, 720p, 480p)
- يختار الدقة المناسبة حسب سرعة النت
- يحمل القطع واحدة واحدة
- **يغير الدقة تلقائياً** لو النت بطأ أو سرع

### 2. نسخ التحميل المباشر
```
uploads/processed/{contentId}/downloads/
├── video_1080p.mp4       # للتحميل بدقة 1080p
├── video_720p.mp4        # للتحميل بدقة 720p
├── video_480p.mp4        # للتحميل بدقة 480p
└── audio.mp3             # الصوت فقط (320kbps)
```

**كيف يشتغل:**
- المستخدم يضغط "تحميل"
- يختار الدقة (1080p, 720p, 480p) أو صوت فقط
- يحمل ملف واحد كامل

### 3. Thumbnail
```
uploads/processed/{contentId}/thumbnail.jpg
```

## 🔄 متى تتم المعالجة؟

### ❌ الطريقة القديمة (بطيئة):
```
رفع → انتظار → معالجة → نشر
```

### ✅ الطريقة الحالية (سريعة):
```
رفع → نشر مباشرة → معالجة في الخلفية
```

**الفائدة:**
- المحتوى يظهر منشور فوراً
- المعالجة تتم مرة واحدة فقط
- الملفات جاهزة للمشاهدة والتحميل

## ☁️ التخزين الخارجي (S3, Cloud Storage)

### الطريقة:
1. **الرفع:** المستخدم يرفع للسيرفر
2. **المعالجة:** السيرفر يعالج الملف (HLS + نسخ التحميل)
3. **النقل:** السيرفر ينقل الملفات للـ Cloud Storage
4. **الحذف:** السيرفر يحذف الملفات المحلية
5. **التشغيل:** المستخدم يشغل من Cloud Storage مباشرة

### مثال (AWS S3):
```
https://your-bucket.s3.amazonaws.com/content/{contentId}/hls/playlist.m3u8
https://your-bucket.s3.amazonaws.com/content/{contentId}/downloads/video_1080p.mp4
```

## 📱 في الأندرويد

### للمشاهدة (ExoPlayer):
```kotlin
val player = ExoPlayer.Builder(context).build()
val mediaItem = MediaItem.fromUri("https://api.com/content/{id}/hls/playlist.m3u8")
player.setMediaItem(mediaItem)
player.prepare()
player.play()
```
**ExoPlayer يختار الدقة تلقائياً!**

### للتحميل:
```kotlin
// المستخدم يختار الدقة
val downloadUrl = when(quality) {
    "1080p" -> "https://api.com/content/{id}/downloads/video_1080p.mp4"
    "720p" -> "https://api.com/content/{id}/downloads/video_720p.mp4"
    "audio" -> "https://api.com/content/{id}/downloads/audio.mp3"
}
```

## 🎬 الخلاصة

**HLS = YouTube-style streaming**
- ✅ دقة تلقائية
- ✅ بث سلس
- ✅ يشتغل على كل الأجهزة

**Download Versions = تحميل مباشر**
- ✅ المستخدم يختار الدقة
- ✅ ملف واحد كامل
- ✅ للمشاهدة بدون نت

**المعالجة = مرة واحدة فقط**
- ✅ عند النشر
- ✅ في الخلفية
- ✅ النتيجة تخزن في Cloud Storage
